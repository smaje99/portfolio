<span class="badge">
  <slot />
</span>

<style>
  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;

    background: var(--gradient-primary-accent);
    color: var(--foreground);
    border: 1px solid var(--border-primary);
    box-shadow: 0 0 0 1px var(--panel-scrim) inset;

    font-size: 0.75rem;
    line-height: 1rem;
    font-weight: 600;
    letter-spacing: 0.01em;

    margin-inline-end: 0.5rem;
    padding: 0.35rem 0.8rem;
    border-radius: 999px;
  }
</style>
