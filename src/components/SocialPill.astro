<a
  {...Astro.props}
  target="_blank"
  rel="noopener noreferrer"
  role="link"
  class="social-pill"
>
  <slot />
</a>

<style>
  .social-pill {
    max-width: fit-content;

    display: flex;
    justify-content: center;
    align-items: center;
    column-gap: 0.5rem;

    background-color: rgb(11 43 49 / 78%);

    padding: 0.75rem 1rem;

    color: var(--secondary-foreground);

    border: 1px solid rgb(66 119 127 / 55%);
    border-radius: calc(infinity * 1px);
    box-shadow: 0 1rem 2.5rem rgb(0 0 0 / 18%);

    font-size: 1rem;
    line-height: 1.5rem;

    transition:
      background-color 180ms ease,
      border-color 180ms ease,
      color 180ms ease,
      transform 180ms ease;

    &:hover {
      background-color: rgb(21 62 68 / 92%);
      border-color: var(--brand-lime-soft);
      color: var(--foreground);
      transform: translateY(-2px);
    }
  }
</style>
